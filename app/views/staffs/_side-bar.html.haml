.side-bar
  .side-bar__header
    %h1
      = current_user.name
    = link_to "編集", edit_user_path(current_user)
  .side-bar__menu
    %p
      = link_to "スタッフ一覧", staffs_path(current_user)
    %p sample
    %p sample
    %p sample
